# sanctum list

List all credentials in the vault.

## Usage

```bash
sanctum list [OPTIONS]
```

## Options

| Flag | Description | Default |
|------|-------------|---------|
| `--json` | Output as JSON array | table format |
| `--type <type>` | Filter by credential type | all |

## Example

```bash
sanctum list
```

```
┌─────────────────────┬──────────┬──────────────────────────┬─────────────────────┐
│ Name                │ Type     │ Description              │ Created             │
├─────────────────────┼──────────┼──────────────────────────┼─────────────────────┤
│ openai/api_key      │ api_key  │ OpenAI production key    │ 2026-02-10 23:15:00 │
│ anthropic/api_key   │ api_key  │ Anthropic API key        │ 2026-02-10 23:15:30 │
│ github/token        │ api_key  │ GitHub PAT               │ 2026-02-10 23:16:00 │
└─────────────────────┴──────────┴──────────────────────────┴─────────────────────┘
```

## Notes

- Lists metadata only — secret values are never shown
- The operation is recorded in the audit log as `credential.list`
